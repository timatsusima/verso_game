# 🗄️ Prisma Migration Guide

## Применение миграции QuestionBank

После добавления модели `QuestionBank` нужно применить изменения схемы к базе данных.

### 📍 Локальная разработка

```bash
# В корне проекта
cd apps/web

# Применить изменения схемы
pnpm db:push

# Сгенерировать Prisma Client
pnpm db:generate
```

### ☁️ Vercel (Production)

Миграция применяется **автоматически** при деплое через `vercel.json`:

```json
"buildCommand": "... && pnpm db:push --accept-data-loss && pnpm db:generate && pnpm build"
```

**Что происходит:**
1. `db:push` — применяет изменения схемы к базе данных
2. `db:generate` — генерирует Prisma Client с новыми типами
3. `build` — собирает Next.js приложение

### 🚂 Railway (Realtime Server)

Миграция применяется **автоматически** при старте контейнера через `Dockerfile`:

```dockerfile
CMD ["sh", "-c", "npx prisma db push --accept-data-loss && node dist/index.js"]
```

**Что происходит:**
1. При каждом запуске контейнера применяется `db:push`
2. Затем запускается realtime сервер

### ⚠️ Важно

- `--accept-data-loss` используется потому что мы используем `db:push` вместо миграций
- Для production лучше перейти на `prisma migrate` (будущее улучшение)
- Обе базы (Vercel и Railway) должны использовать **одну и ту же PostgreSQL базу**

### 🔍 Проверка

После применения миграции проверьте:

```bash
# Открыть Prisma Studio
cd apps/web
pnpm db:studio

# Проверить наличие таблицы QuestionBank
# Должна быть видна в списке моделей
```

### 🐛 Если что-то пошло не так

1. **Ошибка "Table already exists"**
   - Таблица уже создана, можно игнорировать
   - Или удалить таблицу вручную и применить снова

2. **Ошибка подключения к БД**
   - Проверьте `DATABASE_URL` в `.env`
   - Убедитесь что база доступна

3. **Prisma Client не обновился**
   - Удалите `node_modules/.prisma`
   - Запустите `pnpm db:generate` снова
