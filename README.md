# 🎯 TG Duel Quiz

Telegram Mini App для 1v1 дуэлей на знания. Вызывай друзей и соревнуйся!

## ✨ Возможности

- **1v1 Дуэли** — вызови друга на интеллектуальный поединок
- **Ranked Matchmaking** — Premier-style система рейтинга с автоматическим подбором соперников
- **Skill Rating (SR)** — система рейтинга с лигами (Bronze/Silver/Gold/Platinum)
- **Любая тема** — вопросы генерируются AI по выбранной теме
- **Честная игра** — античит система commit-reveal
- **Realtime** — мгновенный отклик через WebSocket
- **Мультиязычность** — поддержка RU/EN
- **История дуэлей** — просмотр последних матчей с фильтрацией
- **Rematch** — запрос реванша после завершения дуэли

## 🏗 Структура проекта

```
tg-duel-quiz/
├── apps/
│   ├── web/            # Next.js Mini App (UI + API)
│   └── realtime/       # Socket.IO сервер
├── packages/
│   └── shared/         # Общие типы и константы
├── .env.example        # Пример переменных окружения
└── README.md
```

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- pnpm 9+
- OpenAI API ключ

### 1. Клонирование и установка

```bash
git clone <your-repo-url>
cd tg-duel-quiz

# Установка зависимостей
pnpm install
```

### 2. Настройка переменных окружения

```bash
# Копируем примеры .env файлов
cp .env.example .env
cp apps/web/.env.example apps/web/.env
cp apps/realtime/.env.example apps/realtime/.env

# Редактируем переменные
# Обязательно укажите:
# - OPENAI_API_KEY
# - JWT_SECRET (одинаковый для web и realtime)
# - TELEGRAM_BOT_TOKEN (если используете реальный Telegram)
```

### 3. Инициализация базы данных

```bash
# Генерация Prisma Client
pnpm db:generate

# Создание базы данных
pnpm db:push
```

### 4. Запуск в режиме разработки

**Терминал 1 — Web App:**
```bash
pnpm dev:web
# Запустится на http://localhost:3000
```

**Терминал 2 — Realtime сервер:**
```bash
pnpm dev:realtime
# Запустится на http://localhost:3001
```

### 5. Открытие приложения

Откройте http://localhost:3000 в браузере.

> 💡 **Режим разработки**: При локальной разработке без Telegram используется mock авторизация.

## 📱 Использование в Telegram

### Настройка Mini App

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Включите Mini App: `/newapp` или `/editapp`
3. Укажите URL вашего задеплоенного приложения
4. Добавьте `TELEGRAM_BOT_TOKEN` в переменные окружения

### Деплой

**Web App (Vercel):**
```bash
cd apps/web
vercel --prod
```

**Realtime (Railway/Render/Fly.io):**
```bash
cd apps/realtime
# Следуйте инструкциям вашего хостинга
```

## 🎮 Игровой процесс

### Unranked дуэли (с друзьями)

1. **Создание дуэли** — выберите тему и количество вопросов
2. **Приглашение** — отправьте ссылку другу
3. **Игра** — отвечайте на вопросы, у вас 60 секунд на каждый
4. **Ускорение** — после первого ответа у второго игрока 10 секунд
5. **Результат** — побеждает тот, кто дал больше правильных ответов

### Ranked дуэли (Matchmaking)

1. **Поиск соперника** — нажмите "Играть на рейтинг"
2. **Matchmaking** — система автоматически найдет соперника по вашему рейтингу
3. **Игра** — стандартный игровой процесс
4. **Изменение рейтинга** — после завершения ваш SR обновится в зависимости от результата
5. **История** — все ranked матчи сохраняются в истории с отображением изменения SR

## 🔐 Античит система

Игра использует commit-reveal схему:
- При создании дуэли генерируется `seed` и `commitHash`
- Правильные ответы НЕ отправляются клиенту до конца игры
- После игры клиент может проверить честность через hash

## 🗄️ База вопросов (Question Bank)

- **Кэширование** — вопросы сохраняются в БД для повторного использования
- **Дедупликация** — через fingerprint (SHA256) предотвращается дублирование
- **Ротация** — вопросы автоматически обновляются каждые 30 дней
- **Генерация** — недостающие вопросы генерируются через OpenAI GPT-4o-mini
- **Валидация языка** — гарантируется единый язык для вопросов и ответов

## 🏆 Система рейтинга (Ranked Matchmaking)

### Skill Rating (SR)

- **Premier-style рейтинг** — Elo-like система с модификаторами
- **Учет факторов** — разница в счете и скорость ответов влияют на изменение SR
- **Rating Deviation (RD)** — для новичков применяется более широкий диапазон изменения
- **Лиги**:
  - 🥉 **Bronze**: < 1200 SR
  - 🥈 **Silver**: 1200-1400 SR
  - 🥇 **Gold**: 1400-1600 SR
  - 💎 **Platinum**: 1600+ SR

### Matchmaking алгоритм

- **Подбор по языку** — игроки подбираются по выбранному языку вопросов
- **Диапазон SR** — начальный диапазон ±200 SR, расширяется каждые 12 секунд (±400, ±600)
- **Таймаут** — максимальное время ожидания 60 секунд
- **Защита** — ограничение повторных матчей между одной парой для предотвращения злоупотреблений

### Режимы игры

- **Unranked** — дуэли с друзьями по приглашению, не влияют на рейтинг
- **Ranked** — дуэли через matchmaking, влияют на рейтинг

## 🛠 API

### Авторизация

#### POST /api/auth/telegram
Авторизация через Telegram initData

### Дуэли

#### POST /api/duel/create
Создание новой дуэли (unranked)

#### GET /api/duel/:id
Получение данных дуэли (без ответов)

#### POST /api/duel/:id/join
Присоединение к дуэли

### Рейтинг

#### GET /api/rating
Получение текущего рейтинга пользователя

### История

#### GET /api/duels/history
Получение истории дуэлей (последние 50 матчей)
- Query параметры: `filter` (all | ranked | friends)

## 🔌 Socket.IO события

### Matchmaking

#### Client → Server
- `mm:join` — присоединиться к очереди matchmaking
  ```typescript
  { language: 'ru' | 'en', token: string }
  ```
- `mm:cancel` — отменить поиск соперника

#### Server → Client
- `mm:status` — статус поиска (searching)
- `mm:found` — соперник найден
  ```typescript
  { duelId: string, opponent: { id, displayName }, isRanked: true }
  ```

### Дуэли

#### Client → Server
- `duel:join` — присоединиться к комнате дуэли
  ```typescript
  { duelId: string, token: string }
  ```
- `duel:answer` — отправить ответ на вопрос
- `duel:rematch` — запросить реванш
- `duel:rematchAccept` — принять реванш
- `duel:rematchDecline` — отклонить реванш

#### Server → Client
- `duel:joined` — успешное присоединение к дуэли
- `duel:state` — синхронизация состояния дуэли
- `duel:start` — начало игры
- `duel:question` — новый вопрос
- `duel:tick` — тик таймера
- `duel:locked` — включен 10с таймер
- `duel:questionResult` — результат вопроса
- `duel:finished` — игра завершена
  ```typescript
  { 
    winner: string | null, 
    scores: { [playerId]: number },
    isRanked: boolean,
    ratingChange?: { before: number, after: number, delta: number }
  }
  ```

## 📦 Стек технологий

- **Frontend**: Next.js 14, React 18, TypeScript, TailwindCSS, Zustand
- **Backend**: Node.js + TypeScript, Next.js API Routes, Socket.IO
- **Database**: Prisma ORM + PostgreSQL (Neon)
- **AI**: OpenAI GPT-4o-mini для генерации вопросов
- **Auth**: JWT + Telegram Mini App
- **Realtime**: Socket.IO для matchmaking и игровых событий
- **Монорепозиторий**: pnpm workspace (apps/web, apps/realtime, packages/shared)

## 🔄 Скрипты

```bash
pnpm dev:web        # Запуск web в dev режиме
pnpm dev:realtime   # Запуск realtime в dev режиме
pnpm build          # Сборка всех пакетов
pnpm db:generate    # Генерация Prisma Client
pnpm db:push        # Применение схемы к БД
pnpm db:studio      # Открыть Prisma Studio
pnpm typecheck      # Проверка типов
```

## 🐛 Известные проблемы и решения

### Проблемы, которые были решены

1. **Race condition при join** — решено через единый in-memory state и синхронизацию `duel:state`
2. **Имена игроков не отображались** — добавлен `getDisplayName()` на сервере
3. **Рассинхронизация языка** — добавлена валидация языка после генерации с retry
4. **Двойной расчет рейтинга** — реализована idempotency для расчета SR

## 📚 Дополнительная документация

- [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md) — руководство по применению миграций Prisma

## 📄 Лицензия

MIT

## 🤝 Contributing

Pull requests приветствуются!

---

Made with ❤️ for Telegram Mini Apps
